version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/kelvin-portfolio/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
